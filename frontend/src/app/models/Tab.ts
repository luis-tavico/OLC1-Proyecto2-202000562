export class Tab {
    ruta: string;
    nombre: string;
    contenido: string;

    constructor(ruta: string, nombre: string, contenido: string) {
        this.ruta = ruta;
        this.nombre = nombre;
        this.contenido = contenido;
    }
}
